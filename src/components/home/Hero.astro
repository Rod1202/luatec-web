---
import GradientText from '../GradientText';
---

<section
  class="hero relative w-full flex flex-col justify-center pb-24 md:pb-32 overflow-hidden"
  style="padding-top: calc(var(--header-height) + 6rem);"
  id="inicio">
    <!-- Hero Content -->
    <div class="hero-content w-full max-w-7xl mx-auto px-6 relative z-10">
        <div class="max-w-3xl mt-4 md:mt-8">
            <h1 class="text-5xl md:text-7xl font-extrabold leading-[1.1] mb-8 text-white">
                La infraestructura <br/>
                que convierte tu 
                <GradientText 
                  client:load 
                  colors={["#06b6d4", "#3b82f6", "#60a5fa", "#06b6d4"]} 
                  animationSpeed={8} 
                  showBorder={false} 
                  className="font-extrabold"
                >
                  visión
                </GradientText> 
                en resultados
            </h1>
            <p class="text-lg md:text-xl text-slate-100 mb-12 max-w-2xl leading-relaxed">
                Diseñamos ecosistemas digitales de alto rendimiento para que tu tecnología sea tu mayor ventaja competitiva.
            </p>
            
            <div class="w-full max-w-xl">
                 <a 
                  class="inline-flex items-center justify-center p-2 pr-6 md:p-2 md:pr-8 rounded-full bg-white text-slate-900 shadow-xl transition-all duration-300 hover:shadow-2xl active:scale-95 group mb-4" 
                  href="/formulario"
                  data-astro-prefetch
                  aria-label="Contáctanos"
                >
                  <div class="flex items-center justify-center w-10 h-10 md:w-12 md:h-12 rounded-full bg-black text-white mr-4 transition-transform duration-300 group-hover:bg-slate-800">
                    <span class="material-symbols-outlined text-[20px] md:text-[24px] transition-transform duration-300 group-hover:translate-x-1">arrow_forward</span>
                  </div>
                  <span class="text-base md:text-lg font-bold tracking-wide">Contáctanos</span>
                </a>
                <p class="mt-4 text-xs text-slate-400 font-medium">
                    Recibe una asesoría técnica personalizada en menos de 24 horas.
                </p>
            </div>
        </div>
    </div>

    <!-- Gradients removed as requested -->

    <script>
        window.addEventListener('scroll', () => {
            const hero = document.getElementById('inicio');
            if (hero) {
                const scrollPosition = window.scrollY;
                hero.style.backgroundPositionY = `${scrollPosition * 0.5}px`;
                // Optional: opacity fade for parallax content if desired
                // hero.style.opacity = `${1 - scrollPosition / 1000}`;
            }
        });
    </script>
</section>
