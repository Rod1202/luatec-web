<section class="relative min-h-screen flex flex-col justify-center pt-32 pb-20 overflow-hidden" id="inicio">
    <!-- Hero Content -->
    <div class="container mx-auto px-6 relative z-10">
        <div class="max-w-4xl">
            <span class="inline-block px-4 py-1.5 rounded-full bg-white/10 text-white text-[10px] mobile:text-xs font-bold tracking-widest uppercase mb-8 border border-white/20 backdrop-blur-sm">
                SOLUCIONES TECNOLÓGICAS CORPORATIVAS
            </span>
            <h1 class="text-5xl md:text-7xl font-extrabold leading-[1.1] mb-8 text-white">
                Infraestructura <br/>
                Tecnológica <br/>
                que Impulsa tu <span class="bg-gradient-to-r from-cyan-400 to-blue-500 bg-clip-text text-transparent">Negocio</span>
            </h1>
            <p class="text-lg md:text-xl text-slate-300 mb-12 max-w-2xl leading-relaxed">
                Diseñamos e implementamos ecosistemas digitales robustos, desde cloud computing hasta ciberseguridad avanzada, para garantizar la continuidad y el crecimiento de tu empresa.
            </p>

            <!-- Registration Form -->
            <div class="w-full max-w-xl">
                <form action="/formulario" method="GET" class="relative group">
                    <div class="absolute -inset-1 bg-gradient-to-r from-blue-600 to-cyan-600 rounded-2xl blur opacity-25 group-hover:opacity-50 transition duration-1000 group-hover:duration-200"></div>
                    <div class="relative flex flex-col md:flex-row items-center bg-slate-900/80 backdrop-blur-xl border border-white/10 rounded-2xl p-2 gap-2">
                        <div class="relative flex-grow w-full md:w-auto flex items-center pl-4">
                            <span class="material-symbols-outlined text-slate-400 mr-3">mail</span>
                            <input 
                                type="email" 
                                name="email"
                                placeholder="Tu correo electrónico profesional" 
                                class="w-full bg-transparent border-none text-white placeholder:text-slate-500 focus:ring-0 py-3 text-sm md:text-base outline-none"
                                required
                            />
                        </div>
                        <button type="submit" class="w-full md:w-auto bg-white text-slate-900 hover:bg-slate-200 font-bold py-3 px-6 rounded-xl transition-all flex items-center justify-center gap-2 whitespace-nowrap">
                            Solicitar información
                            <span class="material-symbols-outlined text-sm">arrow_forward</span>
                        </button>
                    </div>
                </form>
                <p class="mt-4 text-xs text-slate-500 ml-2">
                    Recibe una asesoría técnica personalizada en menos de 24 horas.
                </p>
            </div>
        </div>
    </div>

    <!-- Bottom Gradient Fade -->
    <div class="absolute bottom-0 left-0 w-full h-32 bg-gradient-to-t from-background-dark to-transparent z-10 pointer-events-none"></div>
</section>
