<section class="relative w-full h-full flex flex-col justify-center pt-20 pb-0 overflow-hidden" id="inicio">
    <!-- Hero Content -->
    <div class="container mx-auto px-6 relative z-10">
        <div class="max-w-4xl">
            <span class="inline-block px-4 py-1.5 rounded-full bg-[#053D8A]/30 text-white text-[10px] mobile:text-xs font-bold tracking-widest uppercase mb-8 border border-white/20 backdrop-blur-sm">
                SOLUCIONES TECNOLÓGICAS CORPORATIVAS
            </span>
            <h1 class="text-5xl md:text-7xl font-extrabold leading-[1.1] mb-8 text-white">
                La Infraestructura <br/>
                que convierte tu <span class="bg-gradient-to-r from-cyan-400 to-blue-500 bg-clip-text text-transparent">visión</span> en resultados.
            </h1>
            <p class="text-lg md:text-xl text-slate-100 mb-12 max-w-2xl leading-relaxed">
                Diseñamos y operamos ecosistemas digitales de alto rendimiento. Desde la nube hasta la ciberseguridad, aseguramos que tu tecnología nunca sea un límite, sino tu mayor ventaja competitiva.
            </p>
            
            <!-- Registration Form -->
            <div class="w-full max-w-xl">
                 <form action="/formulario" method="GET" class="relative group">
                    <div class="absolute -inset-1 bg-gradient-to-r from-blue-600 to-cyan-600 rounded-2xl blur opacity-25 group-hover:opacity-50 transition duration-1000 group-hover:duration-200"></div>
                    <div class="relative flex flex-col md:flex-row items-center bg-[#053D8A]/40 backdrop-blur-xl border border-white/10 rounded-2xl p-2 gap-2">
                        <div class="relative flex-grow w-full md:w-auto flex items-center pl-4">
                            <span class="material-symbols-outlined text-blue-200 mr-3">mail</span>
                            <input 
                                type="email" 
                                name="email"
                                placeholder="Tu correo electrónico" 
                                class="w-full bg-transparent border-none text-white placeholder:text-blue-200/70 focus:ring-0 py-3 text-sm md:text-base outline-none"
                                required
                            />
                        </div>
                        <button type="submit" class="w-full md:w-auto bg-[#053D8A] hover:bg-blue-700 text-white font-bold py-3 px-6 rounded-xl transition-all flex items-center justify-center gap-2 whitespace-nowrap shadow-lg shadow-black/20">
                            Solicitar Información
                            <span class="material-symbols-outlined text-sm">arrow_forward</span>
                        </button>
                    </div>
                </form>
                <p class="mt-4 text-xs text-blue-200/80 ml-2">
                    Recibe una asesoría técnica personalizada en menos de 24 horas.
                </p>
            </div>
        </div>
    </div>

    <!-- Gradients removed as requested -->

    <script>
        window.addEventListener('scroll', () => {
            const hero = document.getElementById('inicio');
            if (hero) {
                const scrollPosition = window.scrollY;
                hero.style.backgroundPositionY = `${scrollPosition * 0.5}px`;
                // Optional: opacity fade for parallax content if desired
                // hero.style.opacity = `${1 - scrollPosition / 1000}`;
            }
        });
    </script>
</section>
